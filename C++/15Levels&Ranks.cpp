#include <iostream>

int main() {
    int level;
    level = 10;

    if (level > 10) {
        std::cout << "You are stage 1";
    }
    else if (level > 5) {
        std::cout << "You are stage 2";
    }
    else {
        std::cout << "welldone";
    }
  
}
